# Fraud Detection with Graph Neural Networks

GNN-based fraud detection — modeling transactions as a graph to catch relational fraud patterns.

## Setup

```bash
pip install -r requirements.txt
```

## Status

Work in progress.
